{
    "tasks": [
        {
            "type": "shell",
            "label": "Build project (Debug)",
            "command": "make",
            "args": [
                "-s",
                "clean",
                "main-debug",
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}